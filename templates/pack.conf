<relstorage>
    shared-blob-dir false
    blob-dir {buildout_directory}/var/blobcache/{name}
    blob-cache-size 10mb
    <postgresql>
        dsn dbname='{name}' user='{name}' host='localhost' password='{password}'
    </postgresql>
</relstorage>
